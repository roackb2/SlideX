# SlideX Pitch

SlideX Pitch 是一個專注的簡報工作區：用精確畫布安排內容，以單色 Fill 建立每張投影片的視覺基調，再以可編輯的動態完成播放與輸出。

## 開發

```bash
npm install
npm run dev
```

開啟 `http://localhost:3000`。首頁與 Pitch 會依語言導向 `/en` 或 `/zh-TW`，工作區位於 `/workspace/pitch`。

## 驗證

```bash
npm run lint
npm run build
```
